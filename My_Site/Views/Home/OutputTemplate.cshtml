<div>
    @for(int l = 0; l < Model.PsychicTrusts.Count; l++)
    {
        <h3>Доверие к экстрасенсу №@(l+1): @Model.PsychicTrusts[l] </h3>
    }
</div>

<table>
    <tr>
        <th>
           <h3> Введенные значения </h3>
        </th>
        @for(int i = 0; i < Model.Psychics.Count; i++)
        {
            <th><h3> Предсказания экстрасенсa №@(i+1) </h3></th>
        }
    </tr>

    @foreach (var round in Model.GameRounds)
    {
        <tr>
            <th>
                @round.User
            </th>
            @foreach (var number in round.Psychics)
            {
                <th>
                    @number
                </th>
            }
        </tr>
    }
</table>
